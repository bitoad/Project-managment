# Firecrawl API key (Document Researcher + Procurement external search).
# Không có key -> server tự fallback DuckDuckGo websearch.
# Copy file này thành .env rồi dán key thật vào (.env đã được .gitignore).
FIRECRAWL_API_KEY=
